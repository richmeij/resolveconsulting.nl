# Resolve Consulting

Static site for resolveconsulting.nl. Open `index.html`, or serve this folder:

```
python -m http.server 4173
```

The page is in Dutch and English. It follows the browser language, and a manual choice is remembered in this browser.
